# Segredos NAO ficam aqui - mesmo padrao do projeto (setx na VM):
#   RPA_DETRAF_DESPESA_DB_USER / RPA_DETRAF_DESPESA_DB_PASSWORD
# (o envio de e-mail usa o Outlook Desktop ja logado na VM - sem senha propria aqui,
# igual ao padrao do outlook_standalone.py fornecido)

PERIODO_REF = 202607

# Kill-switch: False = monta o e-mail em memoria (destinatarios/assunto/corpo/anexos) mas
# NAO chama mail.Send(). True = envia de verdade.
PERMITIR_ENVIO_EMAIL = False

# ---- Banco (REAPROVEITADO) ----
HOST_BD_WEBFAT = '10.124.66.77'
PORT_BD_WEBFAT = '3306'
DATABASE_WEBFAT = 'webfat'

# ---- Outlook ----
# TODO: confirmar se e a mesma caixa ja usada para RECEBER Detraf no MVP2
# (ex.: tbr00848.br@telefonica.com / caixa "detrafTBRA.br@telefonica.com" citada no To Be)
# ou se o envio de contestacao deve sair de uma conta diferente.
OUTLOOK_ACCOUNT = ''

# ---- Dados ----
# Pasta onde ja estao a carta de contestacao e o arquivo "..._ENV" (gerados nas etapas
# anteriores do Epico 4 - fora do escopo desta HU).
DIRETORIO_CONTESTACOES = 'F:\RPAs_FRONTEND\RPA_DETRAF_DESPESA\data\Contestacoes'
DIRETORIO_TEMP = 'F:\RPAs_FRONTEND\RPA_DETRAF_DESPESA\data\Temp'
