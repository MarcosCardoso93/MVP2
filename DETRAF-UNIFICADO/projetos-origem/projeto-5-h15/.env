# Segredos NAO ficam aqui - mesmo padrao do projeto (setx na VM):
#   RPA_DETRAF_DESPESA_DB_USER / RPA_DETRAF_DESPESA_DB_PASSWORD
# (o envio de e-mail usa o Outlook Desktop ja logado na VM - sem senha propria aqui)

PERIODO_REF = 202607

# Kill-switch: False = monta o e-mail em memoria mas NAO chama mail.Send(). True = envia de verdade.
PERMITIR_ENVIO_EMAIL = False

# ---- Banco (mesmo do restante do projeto) ----
HOST_BD_WEBFAT = '10.124.66.77'
PORT_BD_WEBFAT = '3306'
DATABASE_WEBFAT = 'webfat'

# ---- Banco de LOGS ----
# TODO: confirmar se este RPA usa a MESMA base de logs do RPA_DETRAF_RECEITA (host .79/'rpa')
# ou se precisa de uma base propria. Valores abaixo replicam o RPA_DETRAF_RECEITA por ora.
HOST_BD_RPA = '10.124.66.79'
PORT_BD_RPA = '3306'
DATABASE_RPA = 'rpa'

# ---- Outlook ----
# TODO: nao existia no .env compartilhado - confirmar a conta correta (ver .env.example)
OUTLOOK_ACCOUNT = ''

# ---- Dados ----
# TODO: nao existia no .env compartilhado - confirmar o caminho real (ver .env.example)
DIRETORIO_CONTESTACOES = 'C:\RPA\Dtraf\EPICO_4_HU15_Envio_Email_Contestacao\EPICO_4_HU15_Envio_Email_Contestacao\data\Contestacoes'
DIRETORIO_TEMP = 'C:\RPA\Dtraf\EPICO_4_HU15_Envio_Email_Contestacao\EPICO_4_HU15_Envio_Email_Contestacao\data\Temp'
